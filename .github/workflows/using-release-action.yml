name: Testing release action
on: [workflow_dispatch]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        uses: jagg-c/hello-world-docker-action@v1
        with:
          who-to-greet: '@jhony.garcia'
